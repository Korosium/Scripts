"use strict";const otp=(()=>{const hotp=(()=>{const counter_to_arr=counter=>{const halfway=counter.toString(16).padStart(16,"0");let arr=[];for(let i=0;i<halfway.length;i+=2){arr.push(parseInt(halfway.slice(i,i+2),16))}return arr;};const truncate=(mac,digits)=>{const offset=mac[mac.length-1]&0xf;const p=mac.slice(offset,offset+4);p[0]&=0x7f;return(parseInt(p.map(x=>x.toString(16).padStart(2,"0")).join(''),16)%10**digits).toString().padStart(digits,"0");};return{generate(secret,counter,digits=6,algo="sha1"){const byte_counter=counter_to_arr(counter);switch(algo.toLowerCase()){case"sha1":case"sha-1":return truncate(sha1.hmac.array(secret,byte_counter),digits);case"sha256":case"sha-256":return truncate(sha256.hmac.array(secret,byte_counter),digits);case"sha512":case"sha-512":return truncate(sha512.hmac.array(secret,byte_counter),digits);default:throw new Error("Wrong hash algorithm");}}}})();const totp=(()=>{const get_current_seconds_since_epoch=()=>parseInt(Date.now()/1000);return{generate(secret,algo="sha1",refresh=30,digits=6,time=get_current_seconds_since_epoch()){const interval=parseInt(time/refresh);const next_window=interval*refresh+refresh;const remaining_seconds=next_window-time;const token=hotp.generate(secret,interval,digits,algo);return{"token":token,"remaining":remaining_seconds}}}})();return{hotp:hotp,totp:totp}})();